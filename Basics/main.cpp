#include <iostream> 
using namespace std;
int main() {
  cout << "Namaste Duniya :-) " <<endl; 
  cout << "With n \n";
  cout << "With endl" << endl;
  cout << "\n"; //This will print a new line and doesnt require endl. This is the same as using endl but it is used inside the string.

  //cout is used to print the output on the console and endl is used to end the line and move to the next line.
  //cout is equivalent of Sop from java and endl with cout is Sopln
  //endl and \n are the same but \n can be used in the string whereas endl is used outside the string.
  //<< means we are inserting the value into the cout object.
  return 0;
}
