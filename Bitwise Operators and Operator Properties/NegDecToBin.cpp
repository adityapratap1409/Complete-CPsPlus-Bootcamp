#include <iostream>
#include <math.h>
using namespace std;
int main(){
   /* int n,ntemp;
    cout<<"Enter a number: ";
    cin>>n;
    ntemp=n;
    int i=0,bit2;
    float answer=0;
    while(ntemp!=0){
        int bit=ntemp&1;
        bit2=~bit;
        answer=answer+bit2*pow(10,i);
        ntemp=ntemp>>1;                                              
        i++;
    }
    answer++;                                 Met Critical logic error,will think about it later
    ntemp=n;
    i=0;
    while(ntemp!=0){
        int bit=ntemp&1;
        bit2=~bit;
        answer=answer+bit2*pow(10,i);
        ntemp=ntemp>>1;
        i++;
    }
    cout<<"Binary representation is: "<<answer<<endl;
    return 0;*/
}